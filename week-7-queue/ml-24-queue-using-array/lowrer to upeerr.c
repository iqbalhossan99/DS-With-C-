#include<stdio.h>
int main()
{
                char lower,upper;
                printf("enter any lower case latter ");
                scanf("%c",&lower);
                upper= toupper(lower);
                printf("uppercase later is %c",upper);

}
